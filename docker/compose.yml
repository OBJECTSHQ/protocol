# Local development services for OBJECTS Protocol.
# Usage: docker compose -f docker/compose.yml up -d
#
# Note: The registry now uses SQLite (no external database service needed).
# This file is retained for future services (e.g., monitoring, relay nodes).

services: {}
