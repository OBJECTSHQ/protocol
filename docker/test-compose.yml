# Test registry for E2E tests.
#
# Requires objects-registry:latest Docker image to be available locally.
services:
  registry:
    image: objects-registry:latest
    environment:
      DATABASE_URL: "sqlite:///data/registry.db"
    ports:
      - "0:8080"
    tmpfs:
      - /data
